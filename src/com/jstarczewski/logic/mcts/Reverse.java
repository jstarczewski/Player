package com.jstarczewski.logic.mcts;

import com.jstarczewski.logic.Element;
import com.jstarczewski.logic.mcts.board.Board;

public class Reverse {

    public static Board reverseMove(Board board, Element element) {

        Element e = new Element(element.toString())

        if (board.getMoves().contains(element)) {

        }


    }

}
